import React from "react";
import PropTypes from "prop-types";
import "./Icon.scss";

const Icon = props => {
  const { icon } = props;

  return (
    <div>
        Icon
    </div>
  );
};

export default Icon;
